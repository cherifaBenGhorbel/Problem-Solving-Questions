This is a simple Node.js script to calculate the total calibration value from a text file document.txt.
Each line in the file contains a combination of characters and digits. 
The script extracts the first and last digits from each line, forms a two-digit number, and sums these numbers to compute the total.
